// This translation unit exists only so the target has a compiled source on every
// platform — which makes the header's layout _Static_asserts part of plain
// `swift build`, including on Linux.
#include "SoupRenderShared.h"
