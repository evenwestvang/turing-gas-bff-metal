// CBFFEcologyShared.c — compilation unit for the layer-1 _Static_asserts in
// BFFEcologyShared.h. Every C compile (including Linux `swift build`) checks
// the layout pins. No runtime symbols are exported.
#include "include/BFFEcologyShared.h"
